%table{:border => "0", :width => '100%'}
  - if @list.length == 0
    = 直近の録音はありません
  - else
    - priv_date = nil
    - isFirst = true
    - @list.each do |item|
      - if item[:update] < Date::today - 30
        - next
      - if priv_date.nil? || priv_date != item[:update].strftime('%m/%d')
        - priv_date = item[:update].strftime('%m/%d')
        - if !isFirst
          - break
        - isFirst = false
        %h4= priv_date
      %p= item[:title]
      %audio{:controls => "", :preload => "", :src => "http://hal.mu/rehearsal_recs/" + item[:file], :style => "width:260px"}
        audio タグ未対応です。
        %a{:href => "http://hal.mu/rehearsal_recs/" + item[:file]}> ここ
        をクリックすると聞けます。
